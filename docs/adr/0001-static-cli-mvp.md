# Use a static Rust CLI for the MVP

World in Layers will render prepared local Source Packs into standalone PNG Scenes through a Windows Rust CLI. This deliberately replaces the earlier GPU, browser-editor, animation, and live-data architecture: the Creator needs reproducible stills now, while those capabilities can be added later without blocking the first Bangkok Scene.
