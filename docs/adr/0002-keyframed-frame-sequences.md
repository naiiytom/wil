# Render a Narrow Keyframed Frame Sequence

The first animated Scene Sequence renders numbered 4K, 30 fps PNG frames from YAML keyframes for layer opacity, translation, and scale. It uses three predefined explanatory phases and leaves video encoding, a timeline editor, scripting, physics, and camera animation outside this milestone, keeping the animation deterministic and source-focused.
